/* declaration */
export interface EntityType {
    /**
     * The identifier for this entity.
     */
    id: string
    /**
     * The type for this entity. This seems to be the same as `id`
     */
    type: string
}